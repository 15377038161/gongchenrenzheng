# DESIGN.md

## 气质与意象
- 关键词：书房、纸墨、朱砂点睛、考究的排版
- 具象场景：午后书房，象牙白书桌上摊开一本排版精良的书，深墨色文字，朱砂红书签夹在页侧；窗外光线柔和，尘埃漂浮，一切安静而克制。
- 这不是又一个"科技蓝 AI 聊天窗"，而是一张被打开成工作台的书页。

## 视觉策略
- 图形语言：细线（1px hairline）分隔而非粗边框卡片；朱砂色小圆点作为列表标记；大面积留白。
- 图像方向：纯排版式欢迎屏，无插画素材，靠字重与字号节奏建立层级。

## Design Tokens
### 色彩
- `--paper`：#FAF7F2（象牙纸，页面底色，意象：书页）
- `--paper-deep`：#F1EBE2（侧栏与浅层区块，意象：书页阴影处）
- `--ink`：#1F2328（主文字，意象：墨）
- `--ink-soft`：#5A5F66（次级文字，意象：淡墨）
- `--ink-faint`：#8A9099（辅助文字/占位，意象：笔痕将尽）
- `--vermilion`：#C8442A（朱砂红，唯一强调色：主按钮、焦点、活跃态、书签线，意象：朱砂印章）
- `--hairline`：#E4DCD0（发丝分隔线，意象：纸的折痕）
- `--dark-ink`：#17191D（iframe 深色容器背景，让嵌入页聚焦）

### 字体
- 标题：Noto Serif SC（衬线，Google Fonts .cn 源），标题级 600 字重
- 正文/界面：Noto Sans SC（无衬线）
- 排版节奏：欢迎标题 ~2.5rem 衬线 + 0.9rem 无衬线说明；中英混排时字间距略放宽（letter-spacing: 0.02em）

### 圆角与阴影
- 圆角：小而克制，卡片 10px，按钮 8px，禁止大圆角"软糖感"
- 阴影：几乎不用；仅悬浮元素用一层极浅的暖灰投影 `0 8px 24px rgba(31,35,40,.08)`

## 布局与响应式
- 桌面三段式：顶部细头栏（56px）+ 左侧能力栏（288px，hairline 右分隔）+ 右侧主区
- 主区两种状态：欢迎态（排版式欢迎屏）与工作态（iframe 撑满，深墨背景聚焦）
- < 960px：侧栏收起为顶部能力横滑区；头栏保留
- 移动端：单列，侧栏折叠为可展开面板

## 交互与状态
- 运动性格：安静、克制；过渡 180ms cubic-bezier(.2,.6,.2,1)
- 悬停：朱砂色文字/图标渐入，禁止位移和缩放
- iframe 加载：深墨底 + 淡墨"研墨中…"加载态；完成后渐显
- 主按钮：朱砂实色底白字；hover 加深至 #A93824
- 焦点可见：2px 朱砂 outline offset 2px

## 设计禁忌
- 禁止科技蓝/蓝紫渐变、玻璃拟态、大圆角软糖卡片
- 禁止用阴影堆层级，一律 hairline + 底色差
- 禁止彩色图标堆砌；图标单色墨色，仅活跃态转朱砂
- 禁止表情符号入正式文案
