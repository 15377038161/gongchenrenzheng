# DESIGN.md

## 项目定位（2026-09 二轮改版后）
- 产品名：工程认证（不起花哨名字）；智能体名称统一为「工程认证」
- 归属：华中科技大学环境科学与工程学院
- Logo：使用华科官方校徽图（vi.hust.edu.cn 标准彩色校徽，透明底 PNG，/hust-logo.png），禁止用文字方块代替校徽
- 功能定位：工程认证材料文档处理工具——上传认证材料（doc/pdf/txt），智能体自动提炼文档内容，可按章节号（1.1、1.2、2.1 等）读取并细分选择章节内容；建议项围绕「列出章节目录 / 读取某章节」组织

## 气质与意象
- 关键词：华科校园、工程认证、学术庄重、清透
- 具象场景：喻家山下的华科校园——临湖建筑、青绿远山、湖面波纹与梧桐叶影；一个安静专业的工程认证问答窗口浮于其上
- 主题色体系沿用「鲸落于海」(#3A67AB) 与「春野信风」(#CEF8DC/#C5E8EE)

## 视觉策略
- 图形语言：浅色卡片 + 1px 冷调分隔线；选项以单选卡片呈现（圆点 + 边框 + hover 湖蓝描边）
- 图像方向：用户选定水彩背景（临湖建筑/青山/湖面/飘叶），opacity 0.42 中等不透明度，中心留白容纳内容
- Logo：vi.hust.edu.cn 官方标准彩色校徽（椭圆三色：华科蓝 + 华科红，椭圆外白底抠除透明、徽内白底保留，/hust-logo.png 512px）
- 动效：背景分层叠层系统（BackgroundEffect）——水纹缓流+扩散水波（右下）、气流线（上缘）、枝叶轻摆（右上真实素材 ±1°）、飘叶（外缘 3 片、≤2 片并发）、雾气呼吸；另有进度条当前步呼吸。纪律：只动画 transform/opacity、中央安全区无动态元素、长周期克制、可整体关闭

## Design Tokens
### 色彩
- `--lake-deep`：#3A67AB（湖蓝主强调：按钮、活跃态、链接）
- `--lake-mid`：#6C8FC4；`--lake-soft`：#BACBE3；`--lake-pale`：#DEF1F5；`--lake-mist`：#C5E8EE
- `--mint`：#CEF8DC（完成态）；`--mint-deep`：#B0E4C0
- `--ink`：#1B2733；`--ink-soft`：#4A5A6E；`--ink-faint`：#8A98A8；`--hairline`：#D8E4EE

### 字体（2026-09 二轮：整体放大）
- 标题：Noto Serif SC 600
- body 基准 15px；Markdown 正文 15px/1.85；对话气泡 15px/8；侧栏会话 13.5px；界面辅助 13px

### 圆角与阴影
- 卡片 12px、按钮/输入胶囊 10~14px、气泡 14px（右下角 4px）

## 组件规范
### 智能体进度条（参照用户参考图重做）
- 横向三步：问题理解 → 知识召回 → 生成回答
- 已完成：淡绿圆(#D3ECD9) + 深绿对勾(#2E7D46)，连接线渐变绿→湖蓝
- 进行中：湖蓝实心圆 + 白色序号，env-breath 呼吸（box-shadow 4px→7px 脉冲）
- 未开始：白底描边空心圆
- 完成后折叠为一行「已完成 N 步处理」+ 可展开明细

### 选项卡片（MarkdownView 卡片化）
- 连续 ≥2 项短列表项（≤30 字、无句读）渲染为纵向卡片组
- 卡片：白底、hairline 1.5px 描边、12px 圆角、内 padding 11×16
- 左侧圆点 16px（hover 变湖蓝）；有序列表带湖蓝序号角标
- hover：湖蓝描边 + 上浮 1px + 轻阴影；点击作为下一条消息发送

### 附件卡片（AttachmentChip）
- 上传后可见文件名/类型图标/大小，一目了然
- 白底 10px 圆角卡片：左侧 8×8 图标底（lake-pale）+ MIME 分类 emoji + 文件名截断 + 大小
- 输入区待发送附件带移除按钮；历史消息附件只读展示

### 输入区
- 圆角 14px 容器：回形针上传按钮 + 麦克风语音按钮 + 自适应 textarea + 发送按钮
- focus 时湖蓝描边 + 3px 光晕；语音收音中按钮湖蓝实心 + 呼吸动效
- 语音：Web Speech API（zh-CN，continuous），转写回填输入框

## 布局与响应式
- 桌面：侧栏 260px（校徽 + 工程认证 + 新建 + 历史）+ 主对话区（max-w-3xl）
- < 960px：侧栏抽屉

## 设计禁忌
- 禁止花哨命名品牌；禁止文字代替校徽 logo
- 禁止科技蓝紫渐变、玻璃拟态滥用
- 禁止高饱和撞色；强调色只有湖蓝体系
- 禁止背景透明度过低（低于 12%）导致华科特色不可辨识
