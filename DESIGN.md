# DESIGN.md

## 项目定位（2026-09 二轮改版后）
- 产品名：工程认证（不起花哨名字）；智能体名称统一为「工程认证」
- 归属：华中科技大学环境科学与工程学院
- Logo：使用华科官方校徽图（vi.hust.edu.cn 标准彩色校徽，透明底 PNG，/hust-logo.png），禁止用文字方块代替校徽
- 功能定位：工程认证材料文档处理工具——上传认证材料（doc/pdf/txt），智能体自动提炼文档内容，可按章节号（1.1、1.2、2.1 等）读取并细分选择章节内容；建议项围绕「列出章节目录 / 读取某章节」组织

## 气质与意象
- 关键词：华科校园、工程认证、学术庄重、清透
- 具象场景：喻家山下的华科校园——临湖建筑、青绿远山、湖面波纹与梧桐叶影；一个安静专业的工程认证问答窗口浮于其上
- 主题色体系沿用「鲸落于海」(#3A67AB) 与「春野信风」(#CEF8DC/#C5E8EE)

## 视觉策略
- 图形语言：浅色卡片 + 1px 冷调分隔线；选项以单选卡片呈现（圆点 + 边框 + hover 湖蓝描边）
- 图像方向：用户选定水彩背景（临湖建筑/青山/湖面/飘叶），opacity 0.42 中等不透明度，中心留白容纳内容
- Logo：vi.hust.edu.cn 官方标准彩色校徽（椭圆三色：华科蓝 + 华科红，椭圆外白底抠除透明、徽内白底保留，/hust-logo.png 512px）
- 动效：背景分层叠层系统（BackgroundEffect）——水纹缓流+扩散水波（右下）、气流线（上缘）、全屏飘叶（36 片、2026-09 密度提升 4 倍：0~95vw 网格+确定性抖动生成，尺寸/时长/透明度分级与 A/B 轨迹交替保持原随机观感，delay 全周期错开避免同帧齐飞；透明度 0.42~0.55 二次加深保证辨识度）、雾气呼吸；另有进度条当前步呼吸。纪律：只动画 transform/opacity、长周期克制、可整体关闭。禁忌：大面积静态枝叶贴角装饰（用户反馈突兀，已移除）
- 背景动效开关（2026-09 移除）：动效常开，侧栏不再提供手动开关

## Design Tokens
### 色彩
- `--lake-deep`：#3A67AB（湖蓝主强调：按钮、活跃态、链接）
- `--lake-mid`：#6C8FC4；`--lake-soft`：#BACBE3；`--lake-pale`：#DEF1F5；`--lake-mist`：#C5E8EE
- `--mint`：#CEF8DC（完成态）；`--mint-deep`：#B0E4C0
- `--ink`：#1B2733；`--ink-soft`：#4A5A6E；`--ink-faint`：#8A98A8；`--hairline`：#D8E4EE

### 字体（2026-09 二轮：整体放大）
- 标题：Noto Serif SC 600
- body 基准 18px（2026-09 三轮适老化放大 +20%）；Markdown 正文 17.5px/1.85；对话气泡 18px/1.75；侧栏会话 16px；界面辅助 15.5px；最小字号不低于 12px（仅装饰性标记）

### 圆角与阴影
- 卡片 12px、按钮/输入胶囊 10~14px、气泡 14px（右下角 4px）

## 组件规范
### 智能体进度条（参照用户参考图重做）
- 横向三步：问题理解 → 知识召回 → 生成回答
- 已完成：淡绿圆(#D3ECD9) + 深绿对勾(#2E7D46)，连接线渐变绿→湖蓝
- 进行中：湖蓝实心圆 + 白色序号，env-breath 呼吸（box-shadow 4px→7px 脉冲）
- 未开始：白底描边空心圆
- 完成后折叠为一行「已完成 N 步处理」+ 可展开明细

### 选项卡片（MarkdownView 卡片化）
- 连续 ≥2 项短列表项（≤30 字、无句读）渲染为纵向卡片组
- 卡片：白底、hairline 1.5px 描边、12px 圆角、内 padding 11×16
- 左侧圆点 16px（hover 变湖蓝）；有序列表带湖蓝序号角标
- hover：湖蓝描边 + 上浮 1px + 轻阴影；点击作为下一条消息发送

### 智能体菜单卡片（MenuCard，2026-09 对照官网单选交互重做）
- 选项为独立白底卡片：hairline 描边、12px 圆角、px-4 py-11px、行高 6
- 左侧 16px 单选圆点：默认空心（ink-faint 描边，hover 湖蓝描边）；选中湖蓝实心 + 白芯
- 选中态：湖蓝 1.5px 描边 + lake-pale 底 + 湖蓝轻阴影 + 右侧「✓ 已选择」标记
- 未选中 hover：上浮 1px + lake-soft 描边 + 轻阴影（对齐 MarkdownView 选项卡片）
- 已回答（answered）：选中项保留高亮，其余降为白 60% 底 + ink-faint 只读
- 菜单问题文字 13.5px font-medium ink-soft

### 附件卡片（AttachmentChip）
- 上传后可见文件名/类型图标/大小，一目了然
- 白底 10px 圆角卡片：左侧 8×8 图标底（lake-pale）+ MIME 分类 emoji + 文件名截断 + 大小
- 输入区待发送附件带移除按钮；历史消息附件只读展示

### 输入区
- 圆角 14px 容器：回形针上传按钮 + 麦克风语音按钮 + 自适应 textarea + 发送按钮
- focus 时湖蓝描边 + 3px 光晕；语音收音中按钮湖蓝实心 + 呼吸动效
- 语音：Web Speech API（zh-CN，continuous），转写回填输入框

## 布局与响应式
- 桌面：侧栏 260px（校徽 + 工程认证 + 新建 + 历史）+ 主对话区（max-w-3xl）
- < 960px：侧栏抽屉
- 登录态（2026-09 二次调整）：不强制登录，页面未登录可正常使用；顶栏右上角为登录入口——未登录显示「超星一键登录」湖蓝主按钮（门户已登录用户点击后 snsapi_base 静默授权无感完成），OAuth 在途显示「登录中…」过渡态，已登录显示用户名（首字头像）+「退出」按钮；登录态经 onAuthStateChange 监听自动刷新；超星登录走 OAuth2.0，邮箱登录能力保留在 auth.ts（external_email 已启用，未挂 UI 入口）
- 输入区动态布局（2026-09 三次修正）：空白对话页时输入框下边距固定 `25vh`（输入框底部距页面底边 25% 视口高，即整体处于页面垂直 75% 处的中间偏下位置）；消息流内层 `min-h-full` 撑满滚动区，欢迎文案区 `justify-end` 下沉至输入框正上方，两者组成整体无遮挡；发送首条消息后下边距归零、欢迎区卸载，输入框贴合页面底部边缘；通过 `transition-[margin]` 500ms `cubic-bezier(0.32,0.72,0.22,1)` 平滑过渡，`motion-reduce` 下禁用过渡
- 输入框占位提示（2026-09 业务定制，后精简）：「请输入工程认证相关问题」

## 设计禁忌
- 禁止花哨命名品牌；禁止文字代替校徽 logo
- 禁止科技蓝紫渐变、玻璃拟态滥用
- 禁止高饱和撞色；强调色只有湖蓝体系
- 禁止背景透明度过低（低于 12%）导致华科特色不可辨识

## 适老化设计（2026-09 三轮，面向老年教师）
- 表单文件上传区：纵向卡片 min-h 112px（原单行约 42px，面积 ≥2.5 倍）；2px lake-soft 虚线边框 + 40px 圆形图标底；hover/拖入湖蓝描边 + 上浮 2px + 阴影；上传完成转实线边框 + lake-pale 底 + 湖蓝圆形对勾
- 用户消息气泡：lake-pale 底 + lake-soft 1px 描边 + 16px 圆角（右下 5px）+ 轻阴影，与助手无底文本区形成方向辨识
- 输入区：16px 圆角容器 + 双层阴影（常态浅阴影，focus 湖蓝光晕叠加）；功能按钮 44×44 触达目标；发送按钮 hover 放大 + 阴影
- 全部字号较二轮整体 +20%：body 18px，气泡 18px，Markdown 17.5px，辅助文字 ≥13.5px；层级对比保持 标题 > 正文 > 辅助 ≥4px 级差
