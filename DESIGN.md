# DESIGN.md

## 气质与意象
- 关键词：碧水学院、清透学术、生态水纹、安静的专业感
- 具象场景：华中科技大学环境科学与工程学院的清晨——喻家湖的水面泛起微光，实验楼的白墙映着天青色，梧桐叶影在窗边轻晃；一只学术气息的对话窗口安静浮在这片水色之上。
- 主题色体系来自用户提供的两张配色卡：「鲸落于海」(#3A67AB 海蓝主导) 与「春野信风」(#CEF8DC/#BCECEF 薄荷青辅助)，水环境意象与学院学科天然契合。

## 视觉策略
- 图形语言：柔和的浅色玻璃卡片、1px 冷调分隔线、圆点列表标记；大面积清透留白
- 图像方向：AI 生成的水环境主题背景装饰（喻家湖水波、生态叶片、水流线条），低透明度铺陈，不干扰对话内容
- 动效：CSS 水波纹呼吸（渐变位移）+ 叶片缓浮（translateY 循环），12~20s 长周期，克制不抢焦点

## Design Tokens
### 色彩
- `--lake-deep`：#3A67AB（湖蓝，主强调色：主按钮、活跃态、品牌、链接，意象：鲸落深海）
- `--lake-mid`：#6C8FC4（中湖蓝，次级强调、图标活跃态）
- `--lake-soft`：#BACBE3（雾湖蓝，侧栏底、卡片描边）
- `--lake-pale`：#DEF1F5（浅水白，输入区/卡片底色）
- `--lake-mist`：#C5E8EE（水雾青，hover 浅层）
- `--mint`：#CEF8DC（薄荷绿，成功态/完成标记，意象：春野）
- `--mint-deep`：#B0E4C0（浅薄荷，思考过程标记）
- `--ink`：#1B2733（深海墨，主文字）
- `--ink-soft`：#4A5A6E（次级文字）
- `--ink-faint`：#8A98A8（辅助/占位文字）
- `--hairline`：#D8E4EE（冷调分隔线）

### 字体
- 标题：Noto Serif SC（衬线，学院气质），标题级 600 字重
- 正文/界面：Noto Sans SC（无衬线）
- 排版节奏：欢迎标题 ~2rem 衬线；正文 14px/行高 1.75

### 圆角与阴影
- 圆角：卡片 12px、按钮 8px、气泡 12px（对话气泡可 16px 圆角一角 4px）
- 阴影：轻投影 `0 2px 12px rgba(58,103,171,.10)`；侧栏无阴影用底色差

## 布局与响应式
- 桌面：左侧栏 260px（会话管理）+ 右侧主对话区
- 侧栏结构：顶部品牌区 → 新建对话按钮 → 历史会话列表（滚动）→ 底部说明
- 主区：顶部标题栏（会话名）→ 消息流（max-w-3xl 居中）→ 输入区（底部固定）
- < 960px：侧栏收起为抽屉（汉堡按钮）

## 聊天界面规范
- 用户消息：右对齐，湖蓝浅底气泡（--lake-soft 40% 透明度）
- 智能体消息：左对齐，白底卡片或纯排版（顶注"环境智能体"小字 + 湖蓝圆点）
- Markdown：标题/加粗/列表/引用/代码/表格完整渲染；表格用浅水底+hairline 描边
- 流式打字机：delta 分片追加渲染，末尾湖蓝竖线光标闪烁
- 思考过程（智能体进度组件）：回复区上方展开式面板，逐条展示思考事件（薄荷绿圆点标记），完成后折叠为"已完成 N 步思考"
- 空状态：衬线欢迎标题 + 建议问题胶囊（hover 湖蓝描边）

## 背景动效
- 主背景：AI 生成水环境装饰图（喻家湖水波+生态元素）低透明度（8%~12%）铺底
- CSS 动效：缓慢渐变位移（background-position 循环）模拟水光流动；装饰叶片元素 translateY 缓浮
- prefers-reduced-motion 时关闭动效

## 设计禁忌
- 禁止科技蓝紫渐变、玻璃拟态滥用、大圆角软糖卡片
- 禁止高饱和撞色；强调色只有湖蓝一个体系
- 禁止表情符号入正式文案
- 禁止动效抢占内容焦点（背景动效必须低对比、长周期）
